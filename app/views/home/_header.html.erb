<!-- Navigation bar -->
<nav class="navbar navbar-expand-lg bg-body-tertiary mb-5 ">
  <div class="w-100 my-2 mx-5 d-flex flex-column">
    <!-- Link to landing page -->
    <div class="d-flex align-items-end">
      <%= link_to image_tag('Dayflow-Avatar.png', :border => 0, :alt => 'a happy girl`s face', :width => 67, :height => 67), root_path, :class => "link-underline link-underline-opacity-0" %>
      <%= link_to "andressa machado", root_path, class:"text-decoration-none fs-2 fw-lighter text-secondary mx-2" %>
    </div>



    <div class="collapse navbar-collapse mt-3 w-100 d-flex justify-content-between align-items-end ms-5" id="navbarSupportedContent">
      <ul class="navbar-nav mb-2 mb-lg-0 p-0">
        <li class="nav-item">
          <%= link_to 'Articles published', articles_path, class:"nav-link"%>
        </li>
        <li class="nav-item">
          <%= link_to 'Create new Article', new_article_path, class:"nav-link"%>
        </li>
        <li class="nav-item" style="display: flex;align-items: center">
          <%= form_tag articles_path, :method => "get" do %>
            <%= text_field_tag :term, params[:term], placeholder: "Search by title/content", class: "px-2"%>
            <%= submit_tag("Search", :name => nil) %>
          <% end %>
        </li>
      </ul>

      <ul class="navbar-nav mb-lg-0 d-flex flex-row gap-3 justify-content-end align-items-center">
        <% if Current.user %>
          <li class="nav-item">
            <%= Current.user.email %>
          </li>

          <li class="nav-item">
            <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-outline-danger" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign Up", sign_up_path, class: "btn btn-success" %>
          </li>

          <li class="nav-item">
            <%= link_to "Login", sign_in_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
